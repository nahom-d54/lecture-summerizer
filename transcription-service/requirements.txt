# Core transcription
openai-whisper>=20231117

# CPU-only PyTorch (much smaller than CUDA version ~200MB vs 2GB+)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# API server
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0

# Note: pyannote.audio is NOT included to save disk space (~2GB+ dependencies)
# Speaker diarization is disabled by default. If you need it and have disk space:
# pip install pyannote.audio>=3.1.0
